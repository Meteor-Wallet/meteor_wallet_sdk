# UI design baseline

For UI changes in this directory, read `UI_STANDARD.md` and follow the user-approved Meteor Connect / Meteor Transfer baseline. Reuse `meteor-connect-layout.styles.ts` and the shared bridge panel instead of duplicating styles. Treat the validated desktop style as the reference and preserve intentional mobile layout differences. Later explicit user requests override the baseline; update the standard when they change it.
