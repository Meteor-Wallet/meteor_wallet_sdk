export enum EFunctionCallMethod_WrapNear {
  near_withdraw = "near_withdraw",
  near_deposit = "near_deposit",
}

export enum EWrapNearToken_ContractId {
  WRAP_TESTNET = "wrap.testnet",
  WRAP_MAINNET = "wrap.mainnet",
}
